<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="flashker.Views.ConversationsPage"
             Title="Wiadomości">
    <ContentPage.Content>
        <StackLayout Padding="10">
            <Label Text="Twoje konwersacje" FontSize="Large" FontAttributes="Bold" />
            <CollectionView x:Name="ConversationsList" SelectionMode="Single" SelectionChanged="OnConversationSelected">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10" Orientation="Horizontal" BackgroundColor="#f5f5f5" Margin="0,5,0,5" >
                            <Image Source="{Binding UserAvatar}" WidthRequest="50" HeightRequest="50" Aspect="AspectFill"/>
                            <StackLayout Padding="10,0">
                                <Label Text="{Binding UserName}" FontSize="Medium" FontAttributes="Bold" />
                                <Label Text="{Binding LastMessage}" FontSize="Small" TextColor="Gray" LineBreakMode="TailTruncation" />
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
